#include <stdio.h>
int main(void) {
	int n,k,flag;
	scanf("%d%d",&n,&k);
    int a[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        flag=0;
        if(a[i]==k)
        {
            flag=1;
            break;
        }
    }
    (flag==1)?printf("YES"):printf("NO");
}

